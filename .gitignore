# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
dist/
build/

# Virtual environments
.venv/
venv/
.env

# IDE
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# Jupyter
.ipynb_checkpoints/

# Project-specific
CLAUDE.md

# --- Data & artefacts -------------------------------------------------------
# Recorded datasets and video are large and are re-recorded on the real cell;
# they must never enter git. (URSim data has zero learning value anyway.)
datasets/
outputs/
data/
*.mp4
*.mkv
*.arrow
*.parquet
*.safetensors
checkpoints/
wandb/

# --- Secrets ----------------------------------------------------------------
# HF / W&B tokens and any local site override that may contain a private IP.
.env.*
!.env.example
*.token
secrets.yaml
config/site.local.yaml
config/site.real.yaml

# NOTE (deliberately NOT ignored, these are deliverables):
#   calibration/*.json   GELLO calibration, committed so a fresh clone works
#   envs/                conda environment definition
#   config/site.yaml     active site config (URSim; contains only lab IPs)
#   docs/                HANDOFF + MIGRATION
#   scripts/             bring-up, smoke tests, verification ladder
